const $value = window.board?.block_value.bind(board);
const $data = window.board?.block_data.bind(board);
const $text = window.board?.block_text.bind(board);
const $cursor_pos = window.page?.get_pointer_pos();
